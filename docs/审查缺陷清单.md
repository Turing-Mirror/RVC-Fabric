# 壳层审查缺陷清单

> **状态**：**high / medium / low 全部已修**（2026-07-29）。
> **范围**：产品壳与相关脚本（广场页 / 模型页广告相关项不在本表，见下方说明）。
> **结论**：38 confirmed / 3 refuted → **38 已修**。
> **用法**：历史记录；新问题请写进 `docs/CONTEXT_HANDOFF.md` §8。

## 统计

| 严重度 | 数量 | 已修 |
|--------|------|------|
| high | 4 | 4 |
| medium | 18 | 18 |
| low | 16 | 16 |
| **合计** | **38** | **38** |

## 已确认缺陷（全部已修）

| # | 严重度 | 位置 | 标题 | 状态 |
|---|--------|------|------|------|
| 0–3 | high | hotkeys / voice_install / more_page / realtime_client | 全局热键、音色包校验、open_bootstrap、pid 身份 | **已修** |
| 4–21 | medium | bootstrap / catalog / gui_update / dock / hotkeys / settings / paths / profiles / realtime / build_release / collect_diagnostics | 见历史提交 | **已修** |
| 22–37 | low | bootstrap / catalog / engine_core / devices / paths / realtime / sample / store / vbcable / sync / dsp | 见历史提交 | **已修** |

### low 收尾批（1.2.0）

| # | 修复摘要 |
|---|----------|
| 23 | 多 pth 共享 index 时仅最后消费者 move 删除源 |
| 24 | sidecar `config.json` 原子写 |
| 26 | reload_devices `wait_seq` + 轮询 status |
| 27 | desktop_dir 读 User Shell Folders / KnownFolder |
| 28 | orphan 清扫 `realtime_worker` 限定本安装 root |
| 30–31 | 录音 auto-stop 回调失败；cancel 不落盘 |
| 32 | catalog_fetch 唯一临时文件 |
| 33 | 封面缩略图 `box_px = px(56)` 一致 |
| 34 | VB-Cable 拒 UAC 不降权启动 |
| 35–37 | 既有：is_junction / EQ set_gains / status.json（更早提交） |

## 已证伪（不要再当 bug 修）

- `launcher/audio_devices.py` prefer_monitor_device 回退到 main_out 的契约指控 — **refuted**
- `launcher/online/downloader.py` 无 sha256 时接受 Git LFS pointer — **refuted**
- `launcher/rvc_launcher.py` 下载线程直接调 Tk API — **refuted**

## 与后续提交的关系

- 广场页相关缺陷已在 `0800079` 内修完，**不在本表**。
- DPI/切页 follow-up 已在 `bcb98a1` 落地，**不在本表**。
- **v1.2.0**：Setup + gui_patch **同版本** 发布（壳层审查 backlog 清零）。
